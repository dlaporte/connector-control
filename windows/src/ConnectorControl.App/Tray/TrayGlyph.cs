namespace ConnectorControl.App.Tray;

/// <summary>Catalog §7: powerplug.fill normally, exclamationmark.triangle.fill while an apply awaits retry.</summary>
public enum TrayGlyph
{
    Plug,
    Warning,
}

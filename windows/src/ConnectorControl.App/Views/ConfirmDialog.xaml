<Window x:Class="ConnectorControl.App.Views.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Connector Control"
        Width="440" SizeToContent="Height" ResizeMode="NoResize" ShowInTaskbar="False"
        Background="{DynamicResource SolidBackgroundFillColorBaseBrush}">
  <StackPanel Margin="20">
    <TextBlock x:Name="MessageText" FontSize="14" FontWeight="SemiBold" TextWrapping="Wrap" />
    <TextBlock x:Name="InformativeText" Style="{StaticResource CaptionText}" FontSize="13" Margin="0,8,0,0" />
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
      <Button x:Name="CancelButton" MinWidth="80" IsCancel="True" Click="OnCancel" />
      <Button x:Name="PrimaryButton" MinWidth="80" Margin="8,0,0,0" IsDefault="True" Click="OnPrimary" />
    </StackPanel>
  </StackPanel>
</Window>

<views:DialogWindow x:Class="ConnectorControl.App.Views.UpdateDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:ConnectorControl.App.Views"
        Title="Software Update"
        Width="520">
  <StackPanel Margin="20">
    <TextBlock x:Name="Headline" FontSize="14" FontWeight="SemiBold" TextWrapping="Wrap" />
    <TextBlock x:Name="Detail" Style="{StaticResource CaptionText}" FontSize="13" Margin="0,6,0,10" />
    <Border BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="4" Height="260">
      <FlowDocumentScrollViewer x:Name="Notes" IsToolBarVisible="False" VerticalScrollBarVisibility="Auto" />
    </Border>
    <StackPanel Style="{StaticResource DialogButtons}" Margin="0,16,0,0">
      <Button x:Name="LaterButton" MinWidth="80" IsCancel="True" Click="OnLater" />
      <Button x:Name="InstallButton" MinWidth="140" Margin="8,0,0,0" IsDefault="True" Style="{DynamicResource AccentButtonStyle}" Click="OnInstall" />
    </StackPanel>
  </StackPanel>
</views:DialogWindow>

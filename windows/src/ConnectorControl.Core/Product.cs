namespace ConnectorControl.Core;

/// <summary>The app's display name, shared by every window title, tray tooltip, and notification.</summary>
public static class Product
{
    public const string Name = "Connector Control";
}

# Inherits charset/line-ending/whitespace rules from the root .editorconfig; this file adds
# only the C#-specific conventions the root file has no business knowing about.
[*.cs]
csharp_style_namespace_declarations = file_scoped:warning
csharp_prefer_braces = true:warning
